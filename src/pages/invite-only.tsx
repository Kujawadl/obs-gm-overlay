import { Container } from "@mui/material";
import Head from "next/head";

// TODO: Style this page
export default function InviteOnly() {
	return (
		<>
			<Head>
				<title>Invite-Only | OBS GM Overlay</title>
			</Head>
			<Container fixed>Invite-Only</Container>
		</>
	);
}
